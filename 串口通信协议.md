# 飞控 ↔ 电脑 串口通信协议

## 帧格式

```
AA  FF  [类型]  [数据长度]  [数据...]  [校验]
```

- **帧头**：固定 `AA FF`
- **类型**：功能码，区分数据用途
- **数据长度**：数据字段的字节数
- **数据**：实际内容
- **校验**：前所有字节累加和 & 0xFF

---

## 飞控 → 电脑

### 已有帧

| 类型 | 数据长度 | 数据格式 | 说明 |
|------|----------|----------|------|
| `0x04` | `0x04` | int32 大端 | 飞行高度（cm） |
| `0x06` | `0x06` | 6字节 ASCII | 解锁信号 |
| `0x12` | `0x12` | 18字节 | IMU（加速度 + 角速度 + 姿态角） |

### 新增帧（识别触发 & 投放命令）

| 完整帧（含校验） | 命令值 | 说明 |
|----------------|--------|------|
| `AA FF 01 01 02 AD` | 0x02 | 飞机到位，开始识别**第1个靶子** |
| `AA FF 01 01 03 AE` | 0x03 | 飞机到位，开始识别**第2个靶子** |
| `AA FF 01 01 04 AF` | 0x04 | 飞机到位，开始识别**第3个靶子** |
| `AA FF 01 01 05 B0` | 0x05 | 飞机到位，开始识别**第4个靶子** |
| `AA FF 01 01 0B B6` | 0x0B | 立刻投放第1次物块 |
| `AA FF 01 01 0C B7` | 0x0C | 立刻投放第2次物块 |
| `AA FF 01 01 0D B8` | 0x0D | 立刻投放第3次物块 |

> 类型字段固定为 `0x01`（程序命令），命令值放在数据字段第1字节。

---

## 电脑 → 飞控

### 位置帧（20Hz 持续发送）

```
AA FF 01 06 [X int16][Y int16][YAW int16] [校验]
```

| 字段 | 类型 | 说明 |
|------|------|------|
| X | int16 小端 | X 坐标，单位 cm |
| Y | int16 小端 | Y 坐标，单位 cm |
| YAW | int16 小端 | 偏航角，单位 °（整数度，例：122 = 122°） |

示例：`AA FF 01 06 11 00 2D 00 7A 00 B3`（X=17cm, Y=45cm, YAW=122°）

---

### 任务状态帧（10Hz 持续发送）

```
AA FF 02 02 [task_state] [landing_state] [校验]
```

#### task_state 定义

| 值 | 说明 |
|----|------|
| `0x01` | 初始状态，等待二维码 |
| `0x02` | 二维码已确认，飞控可起飞 |
| `0x03` | 靶1识别完成，**不匹配**，不降落 |
| `0x04` | 靶1识别完成，**匹配**，降落 |
| `0x05` | 靶2识别完成，**不匹配**，不降落 |
| `0x06` | 靶2识别完成，**匹配**，降落 |
| `0x07` | 靶3识别完成，**不匹配**，不降落 |
| `0x08` | 靶3识别完成，**匹配**，降落 |
| `0x09` | 靶4识别完成，**不匹配**，不降落 |
| `0x0A` | 靶4识别完成，**匹配**，降落 |

> 规律：奇数 = 不匹配，偶数 = 匹配（0x02除外）

#### landing_state 定义

| 值 | 说明 |
|----|------|
| `0x01` | 初始（未确定） |
| `0x02` | 左侧降落点 |
| `0x03` | 右侧降落点 |

#### 示例（假设右侧降落）

| 帧 | 含义 |
|----|------|
| `AA FF 02 02 01 01 07` | 上电初始状态 |
| `AA FF 02 02 02 03 09` | QR确认，右降落 |
| `AA FF 02 02 03 03 0A` | 靶1不匹配 |
| `AA FF 02 02 04 03 0B` | 靶1匹配 |
| `AA FF 02 02 05 03 0C` | 靶2不匹配 |
| `AA FF 02 02 06 03 0D` | 靶2匹配 |

---

## 交互流程

```
电脑上电扫二维码
    ↓ 扫到后发 task_state=0x02
飞控起飞，飞到靶1上方悬停
    ↓ 飞控发 AA FF 01 01 02 AD
电脑识别靶1，发识别结果（0x03 或 0x04）
    ↓
飞控根据结果决策，飞到靶2上方悬停
    ↓ 飞控发 AA FF 01 01 03 AE
电脑识别靶2，发识别结果（0x05 或 0x06）
    ↓
... 以此类推 ...
    ↓
到投放点，飞控发 AA FF 01 01 0B B6
电脑立刻触发舵机投放，无需回复
```

---

## 波特率

**115200 bps**，串口设备：`/dev/ttyFC`（实际映射到 `/dev/ttyUSB0`）
